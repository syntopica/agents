---
globs: "**/*.ts,**/*.tsx,**/*.js,**/*.jsx,**/*.mjs,**/*.cjs,**/*.py,**/*.php,**/*.rs,**/*.go,**/*.vue,**/*.svelte,**/*.astro,**/*.java,**/*.kt,**/*.rb"
description: Boundaries & placement rules (agent-optimized)
alwaysApply: false
---

# Boundaries & Placement (agent-optimized)

## Goal

Make it obvious where new code belongs so agents can create **many small files** without guessing.

## Decision Tree (Next.js App Router layout)

If you are writing...

- **A route or page** -> `app/**` (route components, layouts, route handlers)
- **Reusable UI** -> `components/<area>/...`
- **State/effects + orchestration logic** -> `hooks/<area>/useXxx.ts`
- **Pure logic** (no React, no IO) -> `utils/<area>/xxx.ts`
- **External boundary** (network/DB/auth/storage) -> `services/<area>/xxx.ts`
- **A shared shape** -> `types/<area>/Xxx.ts`

## Hard Rules

- **One exported symbol per file** (component / hook / function / type).
- **No inline types** outside `types/`.
- **No helpers inside components/hooks**; helpers go to `utils/`.
- **Thin entry layers:** see subsection below.

## Thin entry layers (canonical)

Entry layers (route handlers, controllers, command handlers) must be thin: validate input, call a `services/` (or equivalent) function, return a response. No business logic, no DB/network access directly in the handler.

- **Next.js:** `@rules/nextjs/route-handlers.mdc`
- **Laravel:** thin controllers in `@rules/php/laravel.mdc`
- **Rust/Tauri:** `@rules/rust/rust-tauri.mdc`
- **Go:** `@rules/go.mdc`

## References

- Next.js: `@rules/nextjs/nextjs.mdc`
- Route handlers: `@rules/nextjs/route-handlers.mdc`
- API response shapes: `@rules/nextjs/api-response-shapes.mdc`
- Validation: `@rules/api/validation.mdc`
- Naming/layout: `@rules/core/naming-and-layout.mdc`
- utils vs services: `@rules/backend/services-and-utils.mdc`
- Anti-patterns: `@rules/core/anti-patterns.mdc`
- Vertical-slice (feature folder, public API): `@rules/core/vertical-slice-architecture.mdc`
