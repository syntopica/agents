# Finding catalog

One row per class of defect this loop has actually caught, with the cheapest
probe that detects it. Add a row after a tick finds something the existing
checks would have missed; edit a row when its probe proves noisy. Evidence is
what makes a row credible - keep the numbers.

| Class                                      | Probe                                                                                                   | Evidence it was real                                                                                                                                                  |
| ------------------------------------------ | ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shared component ignores its route context | Read the rail/header text on every route that renders it, not just the primary one                      | A folder route showed "All inboxes 12465" with per-account inbox unread counts above a list of sent mail                                                              |
| Duplicate React keys                       | Read the console ring after a sweep                                                                     | Two navigation entries pointing at `/` were keyed by path; the palette rendered them under one key                                                                    |
| Placeholder chrome on a shipping screen    | Grep rendered text for "not available", "coming soon", "generating"                                     | Two large panels and an attachment preview shipped as placeholders                                                                                                    |
| Theme-unaware third-party surface          | Set the persisted theme, reload, read computed background and colour                                    | A toast library defaulted to its own light surface in a dark app                                                                                                      |
| Filtered content with an unfiltered canvas | Read the computed background of the root element inside the frame                                       | A dark-adapted email sat inside a white border: the body background propagated to the canvas, which is painted outside the body's filter                              |
| Mojibake from a mislabelled charset        | Grep rendered and stored text for `Ã`, `â€`, `Â`                                                        | 252 of 120,458 stored bodies, across senders a decade apart                                                                                                           |
| Store contention read as a slow query      | Time the same work in-app and out-of-app; if they differ by an order of magnitude, take a stack sample  | 158s in-app against 0.4s through the CLI, every thread parked on one read lock                                                                                        |
| Background work with no duty cycle         | Time the periodic job, compare against its own interval                                                 | A full scan every two minutes on a store where one scan took minutes                                                                                                  |
| Unbacked-off retry loop                    | Count identical log lines per minute                                                                    | 1,308 credential warnings in 35 minutes, once per account per 60s                                                                                                     |
| Counting work the screen never shows       | Read the query behind a summary panel against what the panel renders                                    | A five-table count served a view that displayed two of the numbers                                                                                                    |
| Per-row calls behind one screen            | Read the call counter after a navigation                                                                | -                                                                                                                                                                     |
| Missing interactive affordances            | Read `getComputedStyle(el).cursor` for every visible enabled clickable on every route, anchors excluded | Tailwind v4 Preflight sets buttons to `cursor: default`; 29 controls across Today, Settings and the sidebar against 128 that carried `cursor-pointer`                 |
| Selected state hardcoded to one theme      | Read the computed background of the selected item and of an unselected sibling, in both themes          | A selected filter chip was `bg-slate-900` with no `dark:` variant, so in dark it sat at the same hue as its unselected siblings and its border was darker than theirs |
